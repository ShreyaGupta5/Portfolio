{
  "name": "shreya-gupta-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack portfolio for Shreya Gupta deployed as one Render web service.",
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "build": "node scripts/verify-assets.js",
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
